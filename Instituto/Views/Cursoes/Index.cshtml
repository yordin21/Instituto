@model IEnumerable<Instituto.Models.Curso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripciónCurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DuraciónCurso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripciónCurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DuraciónCurso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCurso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCurso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCurso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
